# V1 Final requirements audit

This read-only review follows the completed local V1 Final handoff. It inspects
the current instructions, queue, production recipe, final verification receipt,
retained corpus ledger, implementation checkpoints, hardware acceptance, and
remote repository state. No build, package application, emulator, old fixture,
percentage calculation, or neutral-image inspection is repeated.

## Findings

| Requirement | Evidence inspected | Finding |
| --- | --- | --- |
| Private version-controlled project and durable records | `gh repo view` reports `isPrivate: true`; local HEAD and `origin/main` both identify `8f73a84158700f64075480fdee6c1a9a4d72bbd5`; current README, queue, specs, and checkpoints are tracked | Met for the delivered V1 work; no public upload or visibility change |
| Halfwidth/proportional framework and complete displayed names/options | English-runtime storage/reader contract, font-edge checkpoint, RC4 font-memory checkpoint, and human acceptance | Implemented, with scoped native checks and accepted reported appearance fixes |
| Dialogue, names, items, letters, and other inventoried text applied | Base recipe's complete resource/integration/finishing stages; classic-letter and residual-general evidence; retained per-record ledger | Inventoried Japanese-bearing records are applied. The three remaining positive-weight ledger flags are the already classified zero-filled structural slots, not untranslated phrases |
| Embedded interface and diagnostic findings | Current V1 finding table, menu follow-up, correction-run receipt, and final diagnostic receipt | All tracked V1-01 through V1-29 findings have implementations included in the final artifact |
| GameCube intent and native game identity | English-runtime contract, source-bound classic-letter text/breaks, full first-job conversation corrections, Shrine correction, source-provenance notes, and user acceptance | Complete text and deliberate continuations are preserved in the inspected paths; native identities/rules remain. Individual unreported layouts are not exhaustively human-reviewed |
| Stability and saves | First-job native comparison results, RC4 allocation-failure correction and memory guards, explicit human acceptance, and final save notes | Reported crashes/loops are fixed; ordinary save/restart/reload is accepted. Exact RC8/final loading directions remain expected, not independently tested |
| English title, screen/building images, and GC-style keyboard | Complete 28-stage artwork recipe, subsequent correction recipe, final manifest scope, and acceptance of all reported visual/editor fixes | Implemented. Preserve intentional lucky-bag decoration and native shrine. The user's excluded neutral-image sweep is not a completion task |
| Docker/source reproducibility | Makefile and 61-stage base, 28-stage artwork, nineteen-stage correction, and final data-only construction records | Each component has recorded passing construction. The composed 109-stage command is not claimed as a fresh whole-chain run and is not queued for replay |
| Patch-only V1 Final handoff | Final package checkpoint, packager implementation, final `verification.json`, and offline instructions | Met locally: exact final ROM reconstruction passed, ten-file patch ZIP prepared, hashes/revisions/requirements/compatibility documented, and no ROM included in the archive |
| Exhaustive whole-game review and hardware/seasonal acceptance | Corpus inventory limits, validation matrix, and human acceptance scope | Not proven. This does not reopen accepted fixes or block the supplied build; broader player testing remains explicit |
| Public publication and redistribution decisions | Source/provenance review and release checklist | Not complete or authorised by private packaging. Source/patch publication decisions remain separate; no permission to distribute Nintendo content is inferred |
| N64-inspired V2 keyboard | V2 specification and user ordering | Deferred; not silently substituted for, or added to, the V1 deliverable |

## Evidence boundaries

The retained ledger belongs to `build/stall-artwork-01/build.json`, not V1 Final.
It is consulted only to locate outstanding source records; no old percentage is
reported as current. It has no pending name consumers and three flagged entries:
`npc_names:00DA`, `npc_names:00DB`, and `item_10:0ECC`. The residual/classic
checkpoints establish those as zero-filled structural storage. Do not modify
padding or restart counter maintenance to clear them.

The ledger explicitly lacks a complete inventory of embedded interface strings
and text-bearing artwork. Subsequent V1 findings close identified omissions;
neither that history nor a clean issue list proves an exhaustive inventory.
Retain this limit without inventing another concrete bug or restarting the
neutral-artwork review the user excludes.

The final receipt records standalone patcher success, no old-build retesting,
no new gameplay run, ROM SHA-256
`0561182044c1526010ed77b1b9c72ac268794c2f7b615f8fa70c007f366483bf`,
and archive SHA-256
`e00bee7961c09c154db91d6020128d6a4f71ff9739bd4abd114889f7bf4c8aa1`.
The final handoff remains unchanged. A new filename and passing patch application
do not establish new hardware acceptance.

## Next action

No tracked V1 implementation remains to apply. Preserve the delivered build
and its passing checks. New concrete playtest findings can justify targeted
code/text changes; broad speculation, old-build replay, and repackaging cannot.

Public publication requires the user's direction while preserving the private
source repository and recorded provenance limits. Do not upload a patch or
change repository visibility as an automatic continuation. The broader project
is not marked fully achieved merely because its local V1 handoff is complete.
