# V1 human hardware acceptance

## Evidence

In the conversation following the final-V1 remaining-work report, the user
explicitly confirms the ordinary save → restart → reload cycle works, with
many reloads and mostly the same save throughout testing. The user also states:

> I've verified everything I've reported to you has been fixed.

This is human original-hardware evidence from the ongoing EverDrive playtests,
not an inference from emulator results, unchanged formats, or the absence of
new complaints. The confirmation does not supply a particular ROM checksum or
enumerate each version-to-version loading direction. Do not invent those details.

## Accepted scope

- Ordinary in-game saving, restarting, and reloading the existing town: passed
  by the user's repeated playtests. This is no longer an outstanding general
  V1 validation task; do not restart the exhausted gyroid automation setup to
  obtain a second form of proof for the same ordinary workflow.
- All user-reported V1 findings V1-01 through V1-23: fixed and accepted by the
  user. This includes the title, keyboard frame/sounds/key alignment, clock,
  currency, catalogue, repayment, tune editor, letter/board editor and prompts,
  recipient display, font edges, transitions, spaces, and existing-town loading.
- The user's earlier reported v0 conversation-loop and Shrine-label corrections
  fall within the same statement that all reported issues are fixed.

The recipient result accepts the reported defect and the shared implementation;
it does not claim the user separately tested all 216 identities. Likewise, the
save result does not claim a matrix of every old/new candidate combination.

## Separate remaining scope

V1-24 through V1-28 are source-identified menu omissions, not bugs reported by
the user. Their construction checks remain valid; this statement alone does not
establish their ordinary appearance or the reachability of development menus.
The newer RC7/RC8 menu labels are not part of these confirmed hardware sessions.
Preserve prior acceptance for retained fixes without labelling it fresh candidate
execution.

Untested seasonal/travel/Pak cases, undiscovered wording/artwork omissions,
source review, and release preparation keep their actual status. This evidence
does not certify exhaustive gameplay or authorise public distribution.

Historical test checkpoints and archived package manifests retain what was
known when they were made. Their pending human-test statements do not override
this acceptance record or the current progress and bug documents.
