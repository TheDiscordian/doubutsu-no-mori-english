# Resolved stationery and placed-name integration

## Installed scope

`build/resolved-items-pilot` installs 65 additional complete fields: seventeen
stationery names and four rotations each of four totems, seven numbered shirts,
and the walker cicada. All 35 new names/rotations fitting ten bytes also enter
native storage. Totals are 4,462 wider fields and 13,745 ordinary edits.
Every previous English name stays unchanged. The carried cicada receives
explicit provenance without changing its translated bytes or adding credit.

The [specification](../../specs/RESOLVED_ITEM_NAMES.md) records exact paper-ID
selection among repeated quantity groups, unchanged paper-pattern references,
individual totem dot review, and the native carried conversion for numbered
shirts/cicada. Source names, source/reference hashes, actual native conversion,
complete donor wording, and all rotations are checked. No artwork changes.

The matching 8,576-byte article profile covers 4,462 fields, with 82 pending.
The creator remains 58,144 bytes with 848 relocation bytes and unchanged machine
code, symbols, other data, workspace, and memory bounds. The full ROM changes
only native names, the wider name resource, creator article data, and its resident
CRC configuration. Earlier dialogue, letters, actors, reader, font, and saved
layout remain unchanged.
Independent creator builds agree on image, relocation data, and manifest;
the repeat build is retained in `build/resolved-items-creator-repeat`.

## Completed verification

Seven source/evidence/resource/complete-ROM/accounting tests pass in 26.335
seconds. These cover all 65 new fields, all 35 short fields, complete earlier
wording and edit retention, all thirty explicit identities, rejection of missing
or incorrect carried donors, actual Japanese source mutation, shortened/rehash
attempts with metadata removed, complete source reconstruction, the four changed
DMA payloads, the UPS round trip, and exactly 65 new credited IDs. The combined
denominator does not change.

Eight article/creator tests pass normally in 15.093 seconds and under address/
undefined-behaviour sanitizers in 17.052 seconds. These cover all 16-bit native
item IDs, every known complete-name mutation, actual supplied article metadata,
invalid inputs, immutable profile mismatches, constrained generator provenance,
and unchanged creator code/symbols/relocations outside its article data.

Sixteen original-match tests pass in 2.606 seconds, the two frozen sheet-name
native-evidence checks pass in 0.570 seconds, and nine preceding sheet-article
checks pass in 15.862 seconds. Completed native batches are not replayed.
All eight preceding sheet source/resource/ROM/accounting checks also pass;
their log is `build/resolved-items-sheet-retention-tests.log`.
Logs use the `build/resolved-items-` prefix: `tests.log`, `article-tests.log`,
`article-sanitizer-tests.log`, `legacy-matches-tests.log`,
`sheet-evidence-tests.log`, and `sheet-articles-tests.log`.

## Completed native batch

`build/smoke-resolved-items-01` completes the 470-action plan at
`build/resolved-items-scenario.json`. All 157 calls and 149 memory assertions pass
in 471 result records. Every planned call argument, expected return, complete
buffer, and guard matches. Stack/checkpoint restoration, unchanged blank
Flash/Pak saves, and graceful shutdown pass. The batch exercises only the new
identities and their short fields, plus shared loader
boundaries, guards, failure checks, and restoration. Audio and screenshots are
disabled, saves are isolated and initially blank, and game-save writes are
forbidden. Its time limit is 600 seconds. Both frozen evidence checks pass in
0.366 seconds and
reconstruct the exact plan from the complete source-bound names; removing a
required field fails. The result log is `build/resolved-items-native-evidence-tests.log`.
Do not schedule this completed work again.

These name-loader checks do not establish normal item display, newly translated
treasure-item publication, ordinary menu/gameplay behaviour, save/reload, or
hardware acceptance.

## Artifacts

| Artifact | SHA-256 |
| --- | --- |
| Complete ROM, 33,554,432 bytes | `b83f259d4cd0b3745027d86d285699135e0873a181de4f78fdd6656e04f2c5db` |
| UPS, 4,493,230 bytes | `9085576231c8e26a1580db40f8c6434ec375471938acbea064806902abd0b2c9` |
| Wider names | `81ccc7a364d9151530c2d2ea21c3e546fc0e0fd07515d7ee615f6d1c8a0b1501` |
| Ordinary candidates | `d8210a7eef263a0452c7d62b6c1f51fc0d103968ce381e484f65d2194bc4d881` |
| Articles | `44d96659425a8cbe6d9936c61fb7542f101d89bc6ac7f37a830146f650b56f8e` |
| Complete creator | `ce8d6ea7f8a75def27c5a205eb92ada28d2f2e3c8ce5ddc91d57faa73952a0fd` |
| Creator relocations | `95f09d4b0c6ea100caa03cf798ee217836670c4e102ab5538dc2cb7be74b34a1` |
| Native scenario | `2a04f126c688ea3653890bcd5825e1b34939fe0e7d314038c5177fdcde7d54c2` |
| Native results | `2d225ebe7ed71e5aaa21c4fc42844dcb378e8eb130139acb45a15435e16c5fdf` |
| Restored checkpoint | `24fda5c96a635cbbc64d26efd3bb206d32e625dd79ab9472a2c89d66862e90fa` |

Generate resources/candidates with the same complete seasonal-runtime flags as
the sheet-name checkpoint, outputting `build/resolved-items-resource` and
`build/resolved-items-candidates`. Rebuild articles in `build/resolved-items-articles`
and the complete seasonal creator in `build/resolved-items-creator`; all earlier
creator flags, `--mail-glyphs`, and the matching `--item-articles` remain required.
`bash tools/build_resolved_items_pilot.sh` builds the complete ROM and UPS.
`tools/resolved_item_scenario.py` generates the bounded native plan.

## Required continuation

Finish the remaining 82 wider
name fields: native-specific designs, gym-clothing identity disagreements, and
accented names. General strings, remaining letter creators/parts, unused/debug
text, and two NPC-name fields also remain in the combined inventory. Complete
all full-name callers and contextual review; preserve GameCube manual timing and
spacing. Normal save/travel/gameplay validation, patch-only release preparation,
title artwork, GameCube-style keyboard, and actual hardware acceptance remain.

The ordinary candidate rejection files are not the combined remaining-text list:
many of their entries already use verified alternate resources. The combined
ledger identifies 437 still-uncredited `string` entries: 384 of the broader
candidate bank's rejections exceed old entry budgets, and 258 lack same-ID
legacy confirmation. Retain those distinctions when expanding callers or
matching reference wording. Debug/example names, reserve labels, date words,
fortune fragments, shop labels, and grammatical counters require actual source/
consumer review, not deletion from the denominator. Eleven letter signatures
also need review because a complete English dynamic-name-only field has no
static characters for the current credit rule; do not invent static wording just
to change the counter. These are investigation targets, not completed fixes.

Native conversion narrows two remaining identity cases. Placed `item_10:0C2C`
(`ヘラ`) converts to fish `2301`, whose carried Japanese field is `ヘラブナ`.
This is the fish identity, not the unrelated dictionary meaning of a spatula;
the unequal Japanese fields need an explicit source-bound alias review. Placed
`17FC` says blue gym clothes but converts to carried `2414`, whose Japanese label
says red. Placed `1800` says red and converts to carried `2415`, labelled blue.
Preserve those actual conversions and inspect design evidence before resolving
colours. The identical-source alias guard must not be relaxed globally.
